<% content_for :title, "Editing gallery" %>

<div class="wrapper">
  <p class="badge">Gallery</p>
  <h1><%= @gallery.title %> gallery</h1>
  <h2 class="text-lg">Editing</h2>
  <%= render "form", gallery: @gallery %>
  <div class="actions">
    <%= link_to "Back to galleries", galleries_path, class: "btn-link-back" %>
    <%= link_to "Show this gallery", @gallery, class: "btn-link" %>
  </div>
</div>
